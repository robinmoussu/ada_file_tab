Ce dossier contient les fichiers correspondant à l'implémentation d'une file à
l'aide d'un tableau. Les éléments sont enregistrés dans un tableau utilisé
comme un buffer circulaire.

Les fichiers présents dans l'archive sont les suivants :
 - file_tab.ads      : définition du package File_Tab
 - file_tab.adb      : implémentation du package File_Tab
 - file_tab_test.adb : fonction de test du package File_Tab. Modifié par
   rapport au template fournis. Test l'utilisation de deux pile en parallèle
   pour s'assurer du bon fonctionnement.
 - Makefile
 - README

Les cibles du makefile sont :
 - all (défaut) : appelle les cibles install puis run
 - install      : compile les différents fichiers ada
 - run          : lance l'exécutable de test
 - clean        : efface les fichiers générés

Ainsi pour lancer l'exécutable de test, et tester l'implémentation de la
file, il suffit de faire `make` dans le dossier contenant le projet.
